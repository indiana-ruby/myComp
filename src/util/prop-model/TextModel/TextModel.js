import BasePropModel from '../BasePropModel/BasePropModel';
class TextModel extends BasePropModel {
    constructor(name,label,config) {
        super(name,label,config);
    }
}

export default TextModel